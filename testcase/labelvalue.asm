; これはラベルを即値として使う例です
        save 0, label  ; 10 個目の命令についたラベルなので 10 に等しい
        halt
        halt
        halt
        halt
        halt
        halt
        halt
        halt
        halt
label   halt
